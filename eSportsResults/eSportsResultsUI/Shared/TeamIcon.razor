@if(!string.IsNullOrEmpty(Id)) {
    <span class=@($"team-icon t{Id}")>@((MarkupString)Name)</span>
}

@code {
    [Parameter] 
    public string Id { get; set; }
    [Parameter] 
    public string Name { get; set; }
}

